name: External Build
defaults:
  run:
    shell: bash -ileo pipefail {0}
on:
  push:
    paths:
      - ".github/workflows/build.yml"
      - "default/**"
      - "scripts/**"
    branches:
      - "main"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Basic info
        run: |
          echo "Build for ${{ github.ref }} branch in ${{ github.repository }} repository."
          echo "Job triggered by ${{ github.event_name }}, on self-hosted ${{ runner.os }}."
      - name: Check out repository
        uses: actions/checkout@v3
      - name: Prepare and Install
        run: |
          external_version=1.11.0
          source scripts/prepare.sh $external_id
          source ocp/external/spdlog/$external_version/default/install.sh
